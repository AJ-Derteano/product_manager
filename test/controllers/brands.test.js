const { fake } = require('sinon')
const { getAllBrands } = require('../../controllers/brands')


describe('Controller brand', () => {
  test('getAllBrands', async () => {
    /* const brandsList = [{ brand: 'Ekos' }, { brand: 'Homen' }];
    const List = fake.returns(brandsList)
    const getAllBrandsT = getAllBrands({ List }) */

    expect(1).toBe(1)
  })
})